# Voice-Controlled-Bot
A voice-controlled robot uses a phone app to convert speech to text and send commands over Bluetooth. The microcontroller reads these commands, matches them with predefined actions, and drives the motors accordingly. Simple parsing and PWM control enable smooth, responsive movement.
